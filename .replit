# Replit runner for a Node 20 app on port 4000
entrypoint = "server.js"
run = "bash -lc 'npm install --no-audit --no-fund && node server.js'"

[env]
PORT = "4000"

[[ports]]
localPort = 4000
externalPort = 80
